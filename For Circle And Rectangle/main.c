/*prog for to find the area and perimeter of rectangle and area and circumference of circle*/
#include<stdio.h>
#include<stdio.h>
void main()
{
    int l,b,area,perimeter;
    float r,area,circumference;
    printf("Enter any two number\n");
    printf("Enter radius of circle:");
    scanf("%d%d",&l,&b);
    scanf("%f",&r);
    area=l*b;
    perimeter=2*9l+b);
    area=3.14*r*r;
    circumference=2*3.14*r;
    printf("%d\n",area);
    printf("%d",perimeter);
      printf("%f",area);
      printf("%f",circumference);

}
