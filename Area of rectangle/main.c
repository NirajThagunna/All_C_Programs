/*prog for to find the area of rectangle*/
#include<stdio.h>
#include<conio.h>
void main()
{
    int l,b,area;
    printf("Enter any two numbers;");
    scanf("%d%d",&l,&b);
    area = 2*(l+b);
    printf("%d",area);
}
